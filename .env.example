# 复制本文件为 .env 并填入你的 DeepSeek API Key
# cp .env.example .env
#
# 注意：请以 UTF-8 编码保存 .env。
# Windows PowerShell 的 `echo "..." > .env` 会生成 UTF-16，
# 代码已做兼容回退，但推荐直接复制本文件以避免编码问题。

DEEPSEEK_API_KEY=sk-your-key-here
