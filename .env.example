# 复制本文件为 .env 并填入你的配置：  cp .env.example .env
# 注意：请以 UTF-8 编码保存 .env（PowerShell 的 `echo > .env` 会生成 UTF-16；代码已兼容回退，但推荐直接复制本文件）。
#
# 本项目用 openai SDK，兼容任意「OpenAI 兼容」服务商，只改这里即可切换，无需动代码。

# ---- 必填 ----
LLM_API_KEY=sk-your-key-here

# ---- 选填（不填则用 DeepSeek 默认）----
# LLM_BASE_URL=https://api.deepseek.com
# LLM_MODEL=deepseek-v4-flash

# 换服务商示例（取消注释对应两行）：
#   OpenAI:    LLM_BASE_URL=https://api.openai.com/v1     LLM_MODEL=gpt-4o-mini
#   Moonshot:  LLM_BASE_URL=https://api.moonshot.cn/v1    LLM_MODEL=moonshot-v1-8k
#   智谱:      LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4   LLM_MODEL=glm-4-flash
#   本地Ollama: LLM_BASE_URL=http://localhost:11434/v1     LLM_MODEL=qwen2.5

# 兼容旧变量名：若你已有 DEEPSEEK_API_KEY / OPENAI_API_KEY 也能被自动识别。
